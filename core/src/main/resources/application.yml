spring:
    profiles:
        active: dev

    thymeleaf:
        mode: LEGACYHTML5

    mail:
        host: localhost
        port: 1025
        user:
        password:
        protocol: smtp
        tls: false
        auth: false
        from: mail@carrot.re
